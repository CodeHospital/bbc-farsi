<%= form_with model: [:admin, feed], class: "needs-validation" do |f| %>
  <% feed.errors.full_messages.each do |msg| %>
    <div class="alert alert-danger"><%= msg %></div>
  <% end %>
  <div class="mb-3">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= f.label :url, class: "form-label" %>
    <%= f.url_field :url, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= f.label :category, class: "form-label" %>
    <%= f.text_field :category, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= f.label :source, class: "form-label" %>
    <%= f.select :source, Feed::SOURCES, {}, class: "form-select" %>
  </div>
  <div class="mb-3">
    <%= f.label :fetch_hour, "Scheduled fetch", class: "form-label" %>
    <%= f.select :fetch_hour,
          Feed::FETCH_HOURS.map { |hour| [ format("%02d:00", hour), hour ] },
          { include_blank: "Disabled — never auto-fetched" },
          class: "form-select" %>
    <div class="form-text">
      Hour of day (server time) when cron auto-fetches this feed. Disabled by default.
    </div>
  </div>
  <div class="mb-3">
    <%= f.label :autopost_telegram_channel_id, "Autopost to", class: "form-label" %>
    <%= f.collection_select :autopost_telegram_channel_id, TelegramChannel.order(:name), :id, :name,
          { include_blank: "Disabled — never autoposted" },
          class: "form-select" %>
    <div class="form-text">
      Telegram channel this feed's translations autopost to once a translation
      completes. The channel must also be enabled with its own Autopost toggle
      on for delivery to actually happen. Disabled by default.
    </div>
  </div>
  <div class="form-check mb-3">
    <%= f.check_box :enabled, class: "form-check-input" %>
    <%= f.label :enabled, class: "form-check-label" %>
  </div>
  <%= f.submit class: "btn btn-primary" %>
  <%= link_to "Cancel", admin_feeds_path, class: "btn btn-outline-secondary ms-2" %>
<% end %>
