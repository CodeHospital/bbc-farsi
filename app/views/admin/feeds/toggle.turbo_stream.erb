<% if @feed_matches_filter %>
  <%= turbo_stream.replace dom_id(@feed) do %>
    <%= render "feed", feed: @feed,
          telegram_posts_counts_by_feed_id: @telegram_posts_counts_by_feed_id %>
  <% end %>
<% else %>
  <%= turbo_stream.remove dom_id(@feed) %>
<% end %>
