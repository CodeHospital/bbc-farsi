Hi <%= @user.name.presence || @user.username %>,

Someone requested a password reset for your BBC Farsi admin account (<%= @user.username %>).
Open this link to choose a new password:

<%= edit_admin_password_reset_url(token: @token) %>

This link expires in 20 minutes. If you didn't request this, you can safely
ignore this email — your password won't be changed.
