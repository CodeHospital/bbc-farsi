# bbcfarsi Rails App — Plan

## Goal
Convert the `update.rb` script into a full Rails 8 admin web app.

## Architecture

### Models
| Model | Key fields |
|---|---|
| `Feed` | name, url, category, source (bbc/nyt), enabled |
| `Article` | feed, title, url (unique), description, published_at, status |
| `Rewrite` | article, content, model_name, status, error_message |
| `Translation` | rewrite, article, translated_title, translated_body, model_name, prompt_name, status, error_message |
| `TelegramChannel` | name, token, channel_id, enabled, autopost |
| `TelegramPost` | translation, telegram_channel, posted_at, status, error_message |

### Services
- `FeedFetcher` — shared base (HTTParty + Feedjira, host allowlist, `Article.ignorable?` filtering); `BbcFeedFetcher` / `NytFeedFetcher` subclass it with their own `allowed_hosts`
- `FeedIngestor` — fetch enabled feeds (fetcher picked per `feed.source`), upsert Articles, enqueue rewrite Tasks
- `Autoposter` — post active completed Translations to autopost channels
- `ArticleRewriter` / `ArticleTranslator` / `TranslationRefiner` — build LLM chat
  `requests` and `process(responses)`; prompt logic only, no Ollama calls
- `OllamaClient` — builds the admin "debug curl" command (display only)
- `TelegramPoster` — sends translated message to a channel

### Task queue (replaces the background job system)
The Rails app does **no** LLM work and never calls Ollama. Instead it creates
`Task` rows (kind: rewrite / translate / refine). A separate **worker client**
(`worker/worker.rb`, runs where Ollama lives) claims tasks over a protected API,
calls Ollama, and posts results back.

- `Task` — DB-backed queue; polymorphic `target` (Rewrite or Translation);
  lifecycle `pending → claimed → completed/failed`; holds model, server URL, and
  chat `requests`. `complete!` chains the next task (rewrite → translate →
  autopost).
- Worker API (bearer `WORKER_API_TOKEN`): `GET /api/tasks/next`,
  `POST /api/tasks/:id/complete`, `POST /api/tasks/:id/fail`.

### Periodic work (no in-app scheduler)
Driven by an external scheduler (system cron) or the admin UI:
- `bin/rails bbc:fetch` (or the admin "Fetch now" button) — `FeedIngestor.run`
- `bin/rails bbc:autopost` — `Autoposter.run_all`

### Admin (session login via `User` model — admin/editor roles)
- `/admin` — dashboard (counts, recent activity)
- `/admin/feeds` — CRUD, enable/disable toggle *(admin-only)*
- `/admin/articles` — list (filter by feed/status), show, trigger rewrite
- `/admin/rewrites` — list, show, edit content, rerun, edit history
- `/admin/translations` — list, show, edit title/body, post to channel, enable autopost, edit history
- `/admin/telegram_channels` — CRUD, enable/disable, autopost toggle *(admin-only)*
- `/admin/users` — manage admin/editor accounts *(admin-only)*
- `/admin/activity_logs` — system-wide "who did what" audit log *(admin-only)*
- `/admin/password_resets` — public "forgot password?" flow (email a reset link, then set a new password)

### Tech Stack
- Rails 8, SQLite3
- DB-backed `Task` queue + external worker client (LLM work)
- Solid Cache (Rails.cache only)
- Bootstrap 5 CDN (admin UI)
- dotenv-rails (.env loading)
- feedjira + httparty (RSS)
- Ollama via the standalone worker (stdlib net/http — no gem in the app)
- telegram-bot-ruby (Telegram)

## Status
- [x] Rails app init
- [x] Gemfile + bundle
- [x] Migrations + models
- [x] Services
- [x] Jobs
- [x] Routes + controllers + views
- [x] Solid Queue cron config
- [x] CHANGELOG update
- [x] README rewrite with full Ollama setup section
- [x] Multi-server / multi-model comparison: OllamaServer model + CRUD, multi_rewrite / multi_translate article actions, server+model labels on cards, jobs accept server_id/model kwargs, services accept server/model params
- [x] Fix /admin/jobs: add solid_queue adapter in development.rb so jobs are visible; override MissionControl layout with admin sidebar wrapper
- [x] Translate without rewrite: `Article#original_rewrite!` pass-through rewrite (no migration), `translate_original` action + button, "Original" option in multi-translate dropdown, single translate falls back to original
- [x] Process jobs in development: multi-db dev (`primary` + `queue`), `solid_queue.connects_to`, loaded `db/queue_schema.rb` into `storage/development_queue.sqlite3`, `bin/dev` sets `SOLID_QUEUE_IN_PUMA=true` (supervisor in-Puma); README + Background Jobs docs updated
- [x] **Replace the job queue with a pull-based task queue + external worker**: removed Solid Queue / Mission Control / all jobs / `ollama-ai` gem / `queue` db; added `Task` model, protected `/api/tasks` endpoints (`WORKER_API_TOKEN`), `worker/worker.rb` (stdlib-only client), `FeedIngestor` + `Autoposter` services, `bbc:fetch` / `bbc:autopost` rake tasks, and an admin `/admin/tasks` queue UI. Services became request-builders + result-processors. Tests updated (FeedIngestor, ArticleRewriter, Task, Api::Tasks). 78 tests green.
- [x] **Worker reads `worker/.env`**: `worker.rb` loads a `.env` file next to the script on startup via a stdlib-only `load_dotenv` parser (no gem); supports comments, `export` prefix, and quoted values; real env vars take precedence. README updated.
- [x] **Task Queue kind filter + numbered pagination**: `/admin/tasks` now filters by kind (composes with the status filter, active button highlighted, live counts); shared pagination partial renders numbered pages via `pagy.series` with `…` gaps and preserves active query-param filters across page links. Added `Admin::TasksControllerTest`. 82 tests green.
- [x] **Cross-filtered task counts**: status badges show `filtered/total` when a kind is selected and kind badges show `filtered/total` when a status is selected (`@status_counts_in_kind` / `@kind_counts_in_status`); plain totals when no cross filter is active. 84 tests green.
- [x] **Task priority**: new `priority` column (migration `20260608000001`, default 0); `Task.claim_next!` claims by `priority DESC, created_at ASC` (`by_priority` scope); admin ▲/▼ steppers on the queue + show page (`prioritize` action → `Task#reprioritize!`). Worker code unchanged (it calls `claim_next!`). 88 tests green. NOTE: dev/prod DBs still need `bin/rails db:migrate` (not run here per project rules).
- [x] **Task search + bulk priority + stale reclaim**: queue search by target article text (polymorphic-safe), composes with filters & pagination; bulk Raise/Lower/Set priority over checkbox-selected tasks (`bulk_prioritize`, form-attribute association, select-all JS); claimed tasks idle > `Task::STALE_AFTER` (1h) auto-return to pending via `reclaim_stale!` (folded into `claim_next!` + `bbc:reclaim_stale` rake task). 95 tests green.
- [x] **Toggle status/kind filters**: clicking the active status/kind button clears it (back to all); inactive sets it; removed the redundant "All" buttons; active buttons get `aria-pressed`. Filters still compose and preserve the search query. 96 tests green.
- [x] **Translations index filter + sort**: `/admin/translations` gains search (article + Persian title), status/model/active/archived filters, and clickable sortable column headers (asc/desc toggle, ▲/▼ indicator, whitelisted `SORT_COLUMNS` + `Arel.sql`). Sort preserves filters & resets page; filter form preserves sort; `eager_load(:article)` for join-based filter/sort. New `Admin::TranslationsHelper#translation_sort_link`. 106 tests green.
- [x] **Rewrite from translation page**: `/admin/translations/:id` adds a "Rewrite article" button that enqueues a rewrite task for the translation's article (reuses the articles `rewrite` action). 108 tests green.
- [x] **Unified Task-style filters**: Articles/Rewrites/Translations now use toggle filter buttons + count badges + search like the Task Queue, via three shared partials (`_filter_group`, `_filter_toggle`, `_filter_search`) that build links with `url_for(request.query_parameters…)` (compose, preserve sort/search, reset page). Tasks refactored onto the same partials. Controllers compute per-dimension counts. Added Articles + Rewrites controller tests. 119 tests green.
- [x] **Production PostgreSQL via `DATABASE_URL`**: prod runs Postgres (`pg` gem in `:production` group; `sqlite3` in `:development, :test`); `config/database.yml` production `primary` uses `adapter: postgresql` + `url: ENV["DATABASE_URL"]`; Solid Cache folded into the primary DB (removed separate `cache` db, new `create_solid_cache_entries` migration + schema, deleted `db/cache_schema.rb`); Dockerfile uses `postgresql-client`/`libpq-dev` + `BUNDLE_WITHOUT="development test"`; `DATABASE_URL` wired into deploy.yml/.kamal/secrets; README/.env.example updated. Dev/test stay SQLite. NOTE: existing DBs need `bin/rails db:migrate` (not run here per project rules). 119 tests green.
- [x] **Worker model-aware task claiming**: worker calls `GET /api/tags` on Ollama each poll cycle to discover available models, passes them as `models[]` to `GET /api/tasks/next`; `Task.claim_next!(models:)` filters pending tasks by model when list provided (no list → any task); `Api::TasksController#claim` reads `params[:models]` and forwards; 204 returned when no compatible task found; graceful fallback if Ollama unreachable. 125 tests green.
- [x] **Worker standalone Bundler app**: added `worker/Gemfile` (Ruby ~> 3.3, `dotenv ~> 3.0`) so the worker is an independent app with its own dependency manifest; replaced hand-rolled `.env` parser with `dotenv` gem (`Dotenv.load`); run with `bundle exec ruby worker.rb` from `worker/` directory; README updated.
- [x] **Translate dropdown on each rewrite card**: completed rewrite cards on the article show page show a translate card footer — single button when one translate target is configured, `<select>` + button when multiple; `rewrite_id` is pre-filled so the translation targets the specific rewrite. 139 tests green.
- [x] **Simplified rewrite/translate buttons for single target**: article show page renders a direct "Rewrite with `<model>`" / "Translate with `<model>`" button when exactly one server+model is configured; the collapsible multi-target panel only appears when multiple targets exist. 139 tests green.
- [x] **Post to Telegram dropdown on article show page**: each completed translation card footer on `/admin/articles/:id` has a channel `<select>` + "📤 Post" button; already-posted channels show "✓" prefix; `post_to_channel` uses `redirect_back` so it works from any page; `articles#show` batches `@posted_channel_ids_by_translation` in one SQL query. 139 tests green.
- [x] **Posted-article strikethrough + hide_posted filter**: article titles in Articles/Rewrites/Translations/Tasks listings show `posted-title` CSS (strikethrough + dimmed) when `article.status == "posted"`; "Hide posted" toggle filter added to Articles, Rewrites, and Translations index pages (composes with all existing filters and sort). 139 tests green.
- [x] **Action Cable live updates on article show page**: enabled `action_cable/engine`; added `config/cable.yml` (async adapter); created `app/channels/application_cable/` connection + channel; mounted `/cable` in routes; `Task#mark_claimed!`, `complete!`, and `fail!` each call `Turbo::StreamsChannel.broadcast_refresh_to("article_<id>_tasks")`; article show view subscribes via `turbo_stream_from` — page auto-refreshes when worker updates a task.
- [x] **Sortable columns on all listing pages**: generic `ApplicationHelper#sort_link` replaces the translations-specific helper; Articles/Rewrites/Tasks/Translations/Feeds/TelegramChannels/TelegramPosts/OllamaServers index views all have clickable ▲/▼ column headers with whitelisted `SORT_COLUMNS` + `Arel.sql`; sort composes with filters and search, resets pagination; `eager_load` used where join-based sorting is needed. 133 tests green.
- [x] **Public BBC-Persian-style news site**: unauthenticated front end at the site root showing the latest translated/refined Persian news. `NewsController` (`layout "news"`) — `index` shows one story per article (most recent completed, non-archived translation with Persian text, so a finished `prompt_name: "refine"` translation supersedes the one it improved) as a BBC-style lead story + two-column grid; `show` renders the full translated article (RTL, Vazirmatn) with a link to the original BBC source. `root` now → `news#index` (was redirect to `/admin`); added `resources :news, only: %i[index show]`. New `layouts/news.html.erb`, `news/index` + `news/show` views, `NewsHelper` (Persian categories + relative timestamps). Both pages render 200 OK.
- [x] **Article main image on public show page**: `news#show` renders the article's main image from the source page's `og:image` via `ArticleImageFetcher` (SSRF host allow-list, cached in Rails.cache/Solid Cache for a week incl. misses, resolved on demand — no schema change). `NewsControllerTest` added. 154 tests green.
- [x] **Homepage thumbnails + AI featured stories + AI tags** (no migrations, worker-queue AI): (1) `news#index` shows thumbnails via `ArticleImageFetcher.call_many` (cache-first, misses fetched with bounded concurrency). (2) `FeaturedSelector` picks featured stories — heuristic (high-impact categories, newest first) now/as fallback, AI via `bbc:feature` → `feature` Task → cached `featured_article_ids` (3-day TTL) which takes precedence; homepage renders ★ ویژه lead + featured cards. (3) `TagGenerator` + `tag` Task + `bbc:tag` generate Persian topic tags per article, cached `article_tags/<id>` (30-day TTL), shown as chips on `news#show`. `feature`/`tag` Tasks are read-only on their anchor target (`CACHE_RESULT_KINDS`); worker unchanged (kind-agnostic). Added `FeaturedSelectorTest`, `TagGeneratorTest`, feature/tag `TaskTest` cases, news index/show tests. 170 tests green. Run `bin/rails bbc:feature` / `bbc:tag` (then a worker) to populate the AI caches.
- [x] **SEO for the public news site** (no migrations): friendly URLs via `Translation#seo_param` = `"<id>-<persian-slug>"` (id-prefixed so `params[:id].to_i` works on PostgreSQL; `to_param` left as numeric id so admin routes are unaffected); `NewsHelper#news_story_path/url` for public links; `news#show` 301-redirects non-canonical slugs to the canonical URL. News layout `<head>` emits title/description/canonical + Open Graph + Twitter Card (article image when present); `news#show` adds `NewsArticle` JSON-LD. New `GET /sitemap.xml` (homepage + all published stories) and dynamic `GET /robots.txt` (Sitemap directive uses the real host; removed the static `public/robots.txt` placeholder). SEO tests added. 175 tests green; smoke-tested live (friendly links, 301, canonical, JSON-LD, robots, sitemap).
- [x] **Public site redesigned as a Newspaper-style magazine** (tagDiv "Newspaper" demo look): new `layouts/news.html.erb` with top bar (date + social), centered masthead, dark category menu (red active), two-column body (content + `news/_sidebar`), dark footer — RTL/Persian, Vazirmatn, accent `#dd3333`. Homepage = hero block (1 large + 2 stacked overlay cards with category labels/titles over images) + per-category section blocks (`news/_post_item` thumbnail-left lists); sidebar = latest news + category counts. New `GET /category/:slug` (route `category`) filters the main column by feed category; `NewsController#load_chrome` builds shared chrome (`@category`, `@all_stories`, `@sidebar_recent`), `sections_by_category` groups the homepage. Reusable partials `_overlay_card`/`_post_item`/`_sidebar`; helpers `nav_categories`/`story_time`. `news#show` restyled (breadcrumb, overlaid label, full-width figure) + shares sidebar. SEO preserved. SRI added on CDN script/css. Added category + hero tests; index/show tests updated. 181 tests green; smoke-tested live (chrome, hero, sections, category pages, show).
- [x] **Prioritize button on pending tasks on the article page**: `/admin/articles/:id` shows the up/down priority stepper next to any rewrite/translation whose queue task is still pending. `ArticlesController#show` builds `@task_by_target` (Task per rewrite/translation, keyed by `[type, id]`); the card headers render `admin/tasks/_priority_controls` when the task is pending (`prioritize` already `redirect_back`s). Also fixed two `chain_refine` refactor regressions raising `ArgumentError` (`multi_translate` `chain_autopost:`→`chain_refine: false`; `Task#chain_refine!` `ollama_server:`→`server:`). `chain_refine` column/migration `20260617000001` already applied. Added translate→refine chain test + article prioritize-button tests. 184 tests green.
- [x] **Admin House Keeping — abort all pending tasks**: new `/admin/housekeeping` page (sidebar item under Infrastructure, `Admin::HousekeepingController#show`/`abort_pending_tasks`) with an "Abort all pending tasks" button. `Task.abort_pending!` marks every pending task `failed` ("Aborted by admin") and sets its rewrite/translate/refine target to `error` (non-destructive — re-queue from Tasks page); claimed/completed tasks and `feature`/`tag` anchor targets are left untouched. `Admin::HousekeepingControllerTest` added. 180 tests green.
- [x] **Public site brought closer to the tagDiv "Newspaper" demo**: per-category color coding via `NewsHelper::CATEGORY_COLORS` + `category_color`/`category_style` (inline `--cat` custom property drives label + `block-title` accents — world green, business orange, tech blue, science purple, health pink, uk teal, top red); homepage hero became a mosaic "big grid" (1 large lead + up to 4 overlay `tile` cards in a 2×2 grid); homepage category sections became full modules (large `news/_module_lead` lead post with image/title/excerpt + thumbnail list + colored block header + "بیشتر ›" more-link); post-item thumbs hover-zoom; `news#show` article adopts its category color. No schema/route changes; SEO preserved. 181 tests green.
- [x] **Worker status page**: `worker/worker.rb` serves an auto-refreshing status dashboard over a stdlib-only `TCPServer` HTTP server (no framework/gem; added `socket`/`cgi`/`time` requires). New thread-safe `WorkerState` tracks phase (`starting`/`idle`/`processing`/`error`), in-flight task with per-request progress, completed/failed totals, last poll time, and a rolling 50-entry history. Ollama discovery split into `fetch_ollama_models` reporting reachability + models. Endpoints: `GET /` (HTML, light/dark) and `GET /status.json`; 404 otherwise. New `STATUS_PORT` (4567) / `STATUS_BIND` (0.0.0.0) config; busy-port falls back gracefully. README updated. Smoke-tested HTML/JSON/404 live.
- [x] **Login page hides admin menus + untranslated news in English edition** (no migrations): (1) `Admin::SessionsController` now uses `layout false` so its self-contained login view no longer renders inside the admin layout — unauthenticated visitors no longer see the sidebar menus on the login screen. (2) New `ArticleStory` PORO wraps a raw `Article` in the Translation-story interface (`article`/`article_id`/`translated_title`/`translated_body`/`created_at`/`updated_at`/`seo_param`, falling back to original English fields). `NewsController#story_pool` merges recent untranslated, non-archived articles (capped 60, newest-first) into the English (`?lang=en`) edition only; `news#show` resolves `"a<id>-slug"` params to article stories (digit-prefixed stay translation stories). Persian edition unchanged. Added news + sessions controller tests. 191 tests green.
- [x] **Bilingual public site + NewsController caching** (no migrations): the public news site is now bilingual via a `lang` query param (`fa` default / `en`). `NewsController#set_news_lang` resolves the edition and `default_url_options` propagates `?lang=en` across every link; the English edition renders the **original** BBC article (`article.title`/`description`), Persian renders the translation/refinement. New `NewsHelper` edition layer (`news_lang`/`english_edition?`, `news_ui` chrome strings, `story_title`/`story_body`, `category_name` + `CATEGORY_NAMES_EN`, localized `nav_categories`, English `story_timestamp`, `lang_switch_url`); all news views/partials read through it. Layout switches `<html lang/dir>` (RTL Vazirmatn ⇄ LTR Bootstrap), masthead/menu/footer/date, adds a top-bar toggle, emits `og:locale` + reciprocal `hreflang` alternates; `show` JSON-LD uses the edition `inLanguage`; `sitemap.xml` adds `fa`/`en` `xhtml:link` alternates. `latest_translation_per_article` (the expensive per-page story pool) is cached in `Rails.cache`/Solid Cache, keyed on a cheap content version (`story_pool_cache_key`: max translation/article `updated_at` + published count) with a 10-min TTL backstop, recomputing only when data changes (pool is language-agnostic). Bilingual + caching tests added. 188 tests green.
- [x] **Case-insensitive search + news portal search + keyword analytics** (migration `20260618000002`): (1) All admin `LIKE` queries now use `LOWER(col) LIKE LOWER(:q)` for cross-database case insensitivity (articles, rewrites, translations, tasks controllers). (2) New `GET /search` (`news#search`, `news_search_path`) — search bar in the news layout between masthead and main menu; FA edition searches translated title/body, EN edition searches article title/description; results capped at 30, rendered with `_post_item`. (3) `SearchQuery` model + `search_queries` table tracks every search keyword, edition, and results count; `SearchQuery.track!` is error-safe. Admin analytics gains a "Total searches" summary card and "Top search keywords" table (top 20, period-filtered, with FA/EN portal links), visible once the migration is run.
- [x] **Mobile-friendly portal + admin portal shortcuts + click-based priority + analytics** (migration `20260618000001`): (1) News portal layout now fully mobile-responsive: `viewport-fit=cover`, `col-md-8/4` sidebar layout, mobile CSS overrides for hero card height/titles/menu/masthead/sidebar separator, `≤575px` topbar social icons hidden. Admin layout gains a dark mobile top bar with hamburger toggle (`#adminSidebar.show` CSS class + inline JS). (2) Admin articles index and show pages gain 🇮🇷/🌐 portal preview buttons (new `@portal_translation_by_article_id` hash in index action; `@portal_translation` from already-loaded translations in show). (3) `NewsController#show` calls `bump_pending_task_priorities` — one SQL UPDATE bumps all pending Tasks for the article's rewrites/translations by +1 when a reader clicks through. (4) `ArticleView` model + `article_views` table (migration `20260618000001`; run `bin/rails db:migrate` on dev/prod); `track!` extracts country from Cloudflare/CloudFront/CDN headers; `Admin::AnalyticsController#show` at `GET /admin/analytics` shows total views / FA vs EN split / top-15 countries / top-15 articles / daily sparkline across selectable 7/30/90-day windows; migration-missing banner if table absent; `ApplicationHelper#country_flag` + `#country_name`; Analytics added to admin sidebar. 192 tests green.
- [x] **Jalali (Shamsi) dates in Persian news portal** (no migrations): all calendar dates in the Persian edition display in the Jalali calendar with Persian digits (e.g. "۲۸ خرداد ۱۴۰۵"). `NewsHelper` gains `gregorian_to_jalali` (pure-Ruby arithmetic, no gem), `to_persian_digits`, and `jalali_date_string`; `story_timestamp` uses Jalali for the >7-day fallback and Persian digits for relative counts; the topbar date in `layouts/news.html.erb` uses `jalali_date_string`. English edition unchanged. 19 tests green.
- [x] **Cancel button on pending tasks**: `POST /admin/tasks/:id/cancel` + `Admin::TasksController#cancel` calls `fail!("Cancelled by admin")` on a single pending task (marks task `failed`, target `error` — retryable via Retry button). Cancel button (red outline, confirm dialog) added to the task index table and inline on the article show page next to the priority steppers for pending rewrite/translation tasks. 192 tests green.
- [x] **Admin articles search in Farsi**: extended the articles index search to LEFT JOIN `translations` and include `translations.translated_title` in the LIKE clause alongside `articles.title`/`articles.description`; added `.distinct` to suppress duplicate rows from multi-translation articles. Typing Farsi text now finds matching articles.
- [x] **English edition at /en/ URL prefix**: replaced `?lang=en` query param with a `/en/` path prefix for all public English URLs. Routes wrapped in `scope "(:lang)", constraints: { lang: /en/ }` — `lang` is an optional URL path segment, so `default_url_options { lang: "en" }` auto-produces `/en/…` paths. Added `GET /en` (named `en_root`). `lang_switch_url` now swaps the `/en` prefix. `home_path`/`home_url` helpers for edition-aware root links. All `news_path`/`category_path` calls use keyword `id:`/`category:` args. 197 tests green.
- [x] **Parallel worker — N concurrent threads** (no migrations): `WORKER_CONCURRENCY` env var (default 4) spawns N threads, each independently claiming tasks. `WorkerState` tracks all active tasks in `@active_tasks[worker_id]`; `claim_models_refresh?` atomically gates Ollama discovery to one thread per 30 s. `LOG_MUTEX` serialises output with `[worker-N]` prefixes. `trap("INT/TERM")` + `interruptible_sleep` give graceful shutdown. Status page shows a per-slot workers table and history gains a Worker column. 197 tests green.
- [x] **Fragment caching for the news portal** (no migrations): `ArticleStory` PORO gains `cache_key`/`cache_key_with_version` (delegates to article). `NewsHelper#news_sidebar_cache_key` computes a stable sidebar key (sum of updated_at epochs + story count). `cache` blocks added to all story-card partials (`_post_item`, `_overlay_card`, `_module_lead`) keyed on `[story, story.article, news_lang, image_url, …options]`; `_sidebar` wrapped in a single block; `show.html.erb` article body wrapped keyed on `[@translation, @article, news_lang, @image_url, @tags.join(",")]`. `content_for` and breadcrumb nav left outside the fragment. Keys auto-bust on AR `updated_at` — no TTL needed. Off in dev by default (`rails dev:cache` to enable). 197 tests green.
- [x] **SEO / Google / bot / LLM optimisations** (no migrations): (1) Google — `<meta name="robots">`, `<link rel="sitemap">`, `article:published_time/modified_time/section/tag` OG meta, `<meta name="author">`, `@graph` JSON-LD with `NewsArticle` + `BreadcrumbList` on show; `WebSite`+`SearchAction`+`ItemList` on homepage; `Organization` JSON-LD site-wide; `og:image:alt`, `og:locale:alternate`, `hreflang x-default`; hero images `loading="eager" fetchpriority="high"`; `<time datetime>` elements; `preconnect`/`dns-prefetch` for CDN; `theme-color` meta. (2) LLMs — new `GET /llms.txt` (`NewsController#llms`, always English) with llmstxt.org-format site description, section map, URL patterns, JSON-LD inventory, crawling guidance. (3) All bots — robots.txt now has explicit `Allow` + `Disallow /admin /api` blocks for 10 major AI/LLM crawlers. 197 tests green.
- [x] **Admin unpublish/republish for articles and translations**: renamed "Archive article" button to "Unpublish from portal"/"Republish to portal" on the article show page; added "Unpublish" (with confirm) on each completed translation card — hides that translation from the portal so the previous version is shown instead; added "Republish" button on unpublished translation cards and an alert banner on the translation show page; `Translation#unarchive!` + `POST /admin/translations/:id/unarchive` route + controller action; translations index shows "Unpublished" badge and muted row for archived translations. 197 tests green.
- [x] **Friendly slugs for public news URLs** (migration `20260619000001`): public story URLs no longer contain a numeric ID — Persian: `/news/عنوان-خبر`; English article stories: `/en/news/a-title`. `slug` column added to `translations` and `articles` (unique index); `before_save :ensure_slug` in both models generates the slug from the title with `-2`/`-3` collision suffixes. `Translation#seo_param` returns the stored slug (degrades gracefully before migration). `ArticleStory#seo_param` returns `"a-<article-slug>"` to distinguish from translation slugs. `NewsController#show` resolves all four URL formats with automatic 301 redirect to canonical slug. Admin portal-preview buttons updated. New `bbc:backfill_slugs` rake task for existing rows. 197 tests green. NOTE: run `bin/rails db:migrate` then `bin/rails bbc:backfill_slugs` on dev/prod.
- [x] **Unpublish from Farsi portal only — English portal unaffected**: "Unpublish from portal" on the article admin page previously archived the article itself (`article.archived = true`), hiding it from both portals. Now it archives the article's translations (`translations.update_all(archived: true)`), hiding it from the Farsi portal while leaving the article visible in the English edition as an `ArticleStory`. "Republish" unarchives all translations. Button label and state updated accordingly.
- [x] **llmarkt webhook signature verification (`X-Vibe-Signature`)**: per the updated grid API, every webhook is signed with `sha256=<HMAC-SHA256(raw_body, api_key)>`. `Api::LlmCallbacksController` now verifies it constant-time (`Llmarkt.valid_webhook_signature?` / `webhook_signature`, `ActiveSupport::SecurityUtils.secure_compare`) before any processing, returning 401 on missing/invalid/mismatched signatures (callbacks are now authenticated by both the signed URL token *and* the HMAC). Webhook tests cover the valid path and the rejection cases. 212 tests green. Other new doc endpoints (blobs, list/cancel/retry, models, usage) intentionally left unimplemented — the flow only submits jobs + receives webhooks.
- [x] **llmarkt (vibeearning) LLM Grid as primary backend via webhooks** (migration `20260622000001`): enqueued Tasks are POSTed to the llmarkt grid API (`llmarkt.codehospital.com/api/v1/jobs`) and results return via a webhook — making llmarkt the primary LLM path with the Ollama worker as fallback. New `Llmarkt` module (credentials-first config: `llmarkt_api_url`/`llmarkt_api_key`/`app_base_url`/`llmarkt_model_match`, ENV fallback; `enabled?`; `MessageVerifier`-signed webhook tokens), `LlmarktClient` (HTTParty `POST /jobs`, bearer, `model_match` default `family`), and `LlmarktSubmitter` (runs a Task's request chain one job at a time; webhook URL carries a signed `{task_id, key}` token so no job-mapping table is needed; `{{key}}` placeholder substitution; system+user flattened to a single prompt; ignores duplicate/out-of-order callbacks). New `POST /api/llm_callbacks` (`Api::LlmCallbacksController`, token-authenticated, not the worker bearer): `completed` advances the chain / `complete!`s the task, `failed` `fail!`s it. `Task#after_create_commit :submit_to_llmarkt` auto-submits on enqueue (success → `claimed` so the worker skips it; failure/disabled → stays `pending` for the worker fallback). Added `tasks.external_job_id` column (+ index) for the in-flight job id. `.env.example` updated. 211 tests green (14 new). NOTE: run `bin/rails db:migrate` on dev/prod; set credentials/ENV to activate (otherwise `enabled?` is false and the worker path is used unchanged).
- [x] **llmarkt webhook signature verification, task-priority/retry mirrored to llmarkt, per-article task list, IP-geolocation delete, article-view country/city columns**: see CHANGELOG for the full set of small features landed on top of the llmarkt integration (webhook `X-Vibe-Signature` HMAC check; `Task#reprioritize!`/`#retry!` also push to llmarkt via `LlmarktClient.update_job_priority`/`.retry_job`; admin article show page lists every `Task` targeting its rewrites/translations with external job id; `/admin/ip_geolocations` delete action; `article_views.country_name`/`city_name` columns, migration `20260701000002`).
- [x] **Bulk actions on Articles/Rewrites/Translations listing pages**: `/admin/articles` (bulk Rewrite / bulk Translate), `/admin/rewrites` (bulk Rerun), `/admin/translations` (bulk Retranslate / bulk Refine) — row checkboxes + "select all" + a bulk-action bar, mirroring the existing Task Queue bulk-priority UI. Six new POST collection actions (`bulk_rewrite`/`bulk_translate` on articles, `bulk_rerun` on rewrites, `bulk_rerun`/`bulk_refine` on translations), each reusing the same server/model-selection logic as the equivalent single-row action. Extracted the previously Tasks-only "select all + live counter" JS into one generic `data-bulk-*`-attribute-driven function in the admin layout, shared by all four listing pages. 271 tests green (12 new). Smoke-tested live in the browser (bulk Rewrite on 2 articles created 2 tasks and showed the confirmation notice).
- [x] **NYT feeds alongside BBC** (migration `20260703000001`): `feeds.source` column (`bbc`/`nyt`, default `bbc`, `Feed::SOURCES`-validated). Extracted shared RSS-fetch logic into a `FeedFetcher` base class; `BbcFeedFetcher`/new `NytFeedFetcher` are thin `allowed_hosts` subclasses (NYT allowlist: `rss.nytimes.com`, `www.nytimes.com`). `Feed::NYT_FEEDS` (Top Stories/World/U.S./Business/Technology/Science/Health via the classic `rss.nytimes.com/services/xml/rss/nyt/*.xml` feeds) + `Feed.seed_nyt_feeds!`; `db/seeds.rb` seeds both. `FeedIngestor.run` now routes each feed to its fetcher via `feed.source` (unknown source falls back to BBC). Admin `/admin/feeds` gained a "Seed NYT Feeds" button, Source column, and a Source select on the feed form. 281 tests green (10 new). NOTE: dev/prod DBs still need `bin/rails db:migrate` (not run here per project rules).
- [x] **Single-feed Fetch button with a new/updated/skipped-reason report**: `/admin/feeds` row action `POST /admin/feeds/:id/fetch` re-renders the index with a results panel — New/Updated/Skipped badges plus a per-entry table explaining every skip (`Article.ignore_reason` for ignore-pattern matches, "already up to date" for unchanged existing articles, validation errors, or a failure banner if the fetch itself errored). New `FeedFetcher#fetch_with_report` (reports every entry instead of dropping ignored ones; never raises) and `FeedIngestor.run_one(feed)` (existing articles now get their title/description/published_at refreshed via AR dirty-tracking when the feed entry changed; the bulk `.run`/`.fetch` paths are untouched, still create-only). Disallowed-host error text now uses a friendly `source_label` ("BBC"/"NYT") instead of the internal class name, since it's shown in the UI now. 293 tests green (12 new). Smoke-tested live: bad-host feed showed a "Failed: ..." banner; a real NYT Health fetch showed "New: 20, Updated: 0, Skipped: 0", and re-fetching showed "New: 0, Updated: 0, Skipped: 20" with "already up to date" on every row.
- [x] **Full NYT RSS catalog in the seed data**: `Feed::NYT_FEEDS` expanded from the curated 7-feed starter set to all 74 feeds listed on https://www.nytimes.com/rss (World/US/NYRegion/Business/Technology/Sports/Science/Health/Climate/Arts/Style/Travel/Marketplace/Other/Opinion sections, incl. all 14 named columnists) — verified every URL is reachable and on an allowed NYT host. `seed_nyt_feeds!`/`db/seeds.rb`/the admin "Seed NYT Feeds" button are unchanged, they just populate the bigger catalog now. Used the canonical `rss.nytimes.com` Travel URL directly (the page's own `www.nytimes.com` Travel link 301s there, and `FeedFetcher` intentionally doesn't follow redirects); merged the "Environment"/"Climate" duplicate-URL entry into one "Climate" row. 294 tests green (1 new: every NYT_FEEDS URL unique + on an allowed host).
- [x] **Editor accounts, roles, and a "who did what" audit trail** (migrations `20260704000001` create_users, `20260704000002` create_versions — user prompt: "we need to have editors, which are like admins but limited; also we need to know who did what and if someone edits an article, who was it and save every version of the text"): replaced the single `ADMIN_USERNAME`/`ADMIN_PASSWORD` env-var login with a `User` model (`admin`/`editor` role, `has_secure_password`, `active` toggle); `ADMIN_USERNAME`/`ADMIN_PASSWORD` now only bootstrap the first admin via `db/seeds.rb` (also called from the Docker entrypoint). Editors get the full editorial workflow (Articles/Rewrites/Translations/Tasks/Analytics) but are redirected away from infrastructure pages (Feeds, Telegram Channels, Ollama Servers, House Keeping, IP Geolocations, Users, Activity Log) via `Admin::BaseController#require_admin!`; sidebar + dashboard hide those for editors. New `/admin/users` (admin-only CRUD + enable/disable, with a model validation blocking removal of the last active admin) and `/admin/activity_logs` (admin-only, system-wide audit log, filterable by model/user). Added `paper_trail` + `bcrypt` gems; `has_paper_trail` on `Article`/`Rewrite`/`Translation`/`Feed`/`TelegramChannel`(excl. `token`)/`OllamaServer`/`User`(excl. `password_digest`); `ApplicationController` sets `PaperTrail.request.whodunnit` from `current_user` on every request. Rewrite/Translation show pages gained an **Edit history** panel (who + prior text via `version.reify`). Required `config.active_record.yaml_column_permitted_classes` to include `ActiveSupport::TimeWithZone`/`TimeZone` for `reify` to work under Rails' safe YAML loader. 323 tests green (29 new). Smoke-tested live against a disposable copy of the test DB (booted a throwaway server): admin sees Feeds/Users/Activity Log, editor is redirected from all three and the sidebar/dashboard hide those links, editing a rewrite through the real controller action correctly attributed the change to the signed-in user in both the record's history panel and the activity log. NOTE: dev/prod DBs still need `bin/rails db:migrate` (not run here per project rules) then `bin/rails db:seed` to bootstrap the first admin.
- [x] **User emails + "forgot password" via Resend** (migration `20260705000001` add_email_to_users — user prompt: "user should have email so they can reset password, also use resend for sending emails"): added a required, unique, format-validated `users.email` column (`ADMIN_EMAIL` now needed alongside `ADMIN_USERNAME`/`ADMIN_PASSWORD` for the `db/seeds.rb` bootstrap admin and `required_env.rb`); `/admin/users` forms gained an email field. Password reset uses Rails' built-in `generates_token_for :password_reset, expires_in: 20.minutes { password_salt&.last(10) }` (no separate tokens table; folding in `password_salt` auto-invalidates a token the moment the password changes, including by using the token itself). New public `Admin::PasswordResetsController` (`new`/`create` email-entry form — same redirect/message whether or not the email matched an account, so it can't enumerate users; `edit`/`update` via `User.find_by_token_for` to set a new password) + "Forgot password?" link on the login page. New `UserMailer#password_reset` (HTML+text views, `layouts/mailer`) sent via **Resend's SMTP relay** — enabled the previously-commented-out `action_mailer/railtie`; new `MailerConfig` service mirrors `Llmarkt`'s credentials-first-then-ENV pattern (`resend_api_key`/`RESEND_API_KEY`, `sender_email`/`SENDER_EMAIL`); `config/initializers/action_mailer.rb` sets `smtp_settings` (`smtp.resend.com:587`, user `"resend"`, password = the API key) + `default_url_options` (from `Llmarkt.app_base_url`) only when configured, otherwise `perform_deliveries` stays off so the flow still runs without erroring; test env uses the `:test` delivery method; `bin/docker-entrypoint` now also runs `db:seed` on boot. 338 tests green (15 new). Smoke-tested live end-to-end against a disposable copy of the test DB (real server, real generated token, real HTTP requests — no mocking): forgot-password form → request reset (same redirect for a known vs. unknown email, confirming no user-enumeration leak) → edit page renders for a valid token / redirects away for a bogus one → PATCH sets the new password → reusing the same token now redirects away (auto-invalidated) → logged in successfully with the new password → the old password was rejected (422). NOTE: dev/prod DBs still need `bin/rails db:migrate` (not run here per project rules) for the `email` column, and `ADMIN_EMAIL` must be set before the next `db:seed`/first boot; set `RESEND_API_KEY`/`SENDER_EMAIL` (env or credentials) to actually send reset emails.
- [x] **Fixed broken llmarkt job submission + webhook signature header mismatch** (user prompt: "check https://www.llmondemand.com/api-docs/index.html are we using all the api correctly? when user changes task priority, is it updated on llmarkt server?"): cross-checked the integration against the vendor's real OpenAPI spec (`https://www.llmondemand.com/api-docs/v1/swagger.yaml` — same contract as our `llmarkt.codehospital.com` domain). Found that the most recent commit (`7c3d625`, "adding editor role") had added `priority:`/`timeout_seconds:` keyword args to the `LlmarktClient.submit_job` call site in `LlmarktSubmitter#submit_request` without updating `LlmarktClient.submit_job`'s own signature to accept them — every real call raised `ArgumentError: unknown keywords`, silently rescued and rolled back to `pending`, so **every task since that commit was silently falling back to the Ollama worker instead of running on llmarkt**, and priority/retry mirroring to llmarkt never fired (no task ever got an `external_job_id` to mirror against), even though that sync code itself was correct. Root cause of the test-suite blind spot: `LlmarktSubmitterTest` stubbed `LlmarktClient.submit_job` with `->(**) { ... }`, which swallows any keyword mismatch. Fixed `LlmarktClient.submit_job` to accept and forward `priority:`/`timeout_seconds:` (matches the documented `JobCreateRequest` schema); added a submitter-level test that only stubs the HTTP layer (WebMock) and runs the real `submit_job` method, so this class of signature-mismatch bug gets caught going forward. Also found the vendor's docs name the webhook header `X-LLMOnDemand-Signature` while our controller only checked the legacy `X-Vibe-Signature` — now checks both (new name first, old as fallback). 345 tests green (4 new); `bin/rails zeitwerk:check` clean.
- [x] **Fixed initializer boot crash + moved admin bootstrap into Rails credentials** (user hit `NameError: uninitialized constant MailerConfig` running `rake db:migrate`; then asked to move `ADMIN_USERNAME`/`ADMIN_PASSWORD`/`ADMIN_EMAIL` into credentials): root cause was `config/initializers/action_mailer.rb` referencing autoloaded `app/services/` constants (`MailerConfig`, `Llmarkt`) directly at the top level — `config/initializers/*.rb` run before the app's Zeitwerk autoloader is set up, so any such reference raises `NameError` (reproduced the identical failure with the pre-existing, already-working `Llmarkt` constant to confirm it's a general boot-timing gotcha, not a bug in the new file). Fixed by wrapping the initializer body in `Rails.application.config.to_prepare { ... }` (runs once the autoloader is ready, and again on each dev reload); verified via `bin/rails runner` that `ActionMailer::Base`'s settings still end up applied correctly. Separately, added `AdminBootstrap` (`app/services/admin_bootstrap.rb`, same credentials-first-then-ENV pattern as `Llmarkt`/`MailerConfig`) for `admin_username`/`admin_password`/`admin_email`; `db/seeds.rb` reads through it, while `required_env.rb`'s boot gate stays self-contained (checks `Rails.application.credentials.dig`/`ENV` directly, deliberately *not* referencing the autoloaded module, to avoid re-introducing the exact class of bug just fixed). Also discovered (via `rails credentials:show`) the user had already added `admin_username`/`admin_password`/`admin_email` plus a Resend key to credentials themselves, but under the key `sender_email` rather than the `mailer_from_address` name `MailerConfig` was written against — renamed `MailerConfig.from_address` (and `.env.example`/README) to `sender_email`/`SENDER_EMAIL` to match what's actually there. 341 tests green (3 new: `AdminBootstrapTest`). Verified live via `bin/rails runner` in development with **zero** ENV vars set — `AdminBootstrap.configured?`, `MailerConfig.enabled?`, and `ActionMailer::Base.perform_deliveries`/`smtp_settings` all resolved correctly straight from the real encrypted credentials.
- [x] **Prompts moved to the database, with versioning and revert** (migration `20260705000003` create_prompts — user prompt: "move all the prompts to database as activerecord models; allow admin and editors to edit prompts, prompt history should be available with versioning, it's possible to revert back to an earlier version of a prompt; when task and each produced target is connected to a prompt version so it's visible what created it; when generating tasks we use the latest version of a prompt"): new `Prompt`/`PromptVersion`/`PromptVersionUsage` models replace the hardcoded prompt-text constants in `ArticleRewriter`/`ArticleTranslator`/`TranslationRefiner`/`TagGenerator`/`FeaturedSelector` (`ArticleTranslator` previously read the root `prompt` file). `Prompt` is a named slot (`rewrite_body`/`rewrite_title`/`translate`/`refine_title`/`refine_body`/`tag`/`feature`); content lives on immutable `PromptVersion` rows, `current_prompt_version` is what every fresh request is built from (`Prompt.current_version(key)`), editing creates a new version rather than overwriting, and `revert_to!` creates a new version copying an older one's content (append-only history, nothing rewound). `Admin::PromptsController` (`/admin/prompts`, not admin-only — editors can edit) has index/show/edit/update + `revert`, with a version-history panel (author, timestamp, change note, expandable text, revert button) on the show page. Every request built by the five services now carries a `prompt_version_id`; `Task#record_prompt_version_usages` records `PromptVersionUsage` rows linking a `Task` (and, via new `Rewrite#generating_task`/`Translation#generating_task`) to exactly which prompt version(s) produced it — shown via a new shared `admin/shared/_prompt_usages` partial on the Rewrite/Translation/Task show pages. `Prompt.seed_defaults!` (idempotent) seeds the original wording as v1 for all 7 slots, called from `db/seeds.rb` and from `test_helper.rb`'s global `setup`. 382 tests green (12 new). NOTE: production DB still needs `bin/rails db:migrate` then `bin/rails db:seed` to populate the initial prompt content (not run here per project rules).
- [x] **Telegram admin notification bot with a 6-option inline-button workflow** (migration `20260705000002` create_telegram_admin_notifications — user prompt: "create a telegram bot webhook end that when news is ready to publish, sends it to the telegram app, where admin can do these 6 options: 1- request rewrite 2- request retranslation 3- request refine 4- publish to telegram channel 5- publish/unpublish on news portal 6- mark for manual edit by editors"): new `TelegramAdminBot` config module (credentials-first-then-ENV, mirrors `Llmarkt`/`MailerConfig`: `telegram_admin_bot_token`/`telegram_admin_chat_id`/`telegram_admin_webhook_secret`) — a bot separate from the per-channel publishing bots in `TelegramChannel`. `Task#complete!` now calls `TelegramAdminNotifier.notify` after a translate or refine task activates its translation (the same "ready to publish" moment `chain_autopost!` already treats as final), DMing the configured chat with the Persian text preview and an inline keyboard for all 6 requested actions (the 4th opens a channel-picker submenu). `POST /api/telegram_admin/webhook` (`Api::TelegramAdminController`, authenticated via the `X-Telegram-Bot-Api-Secret-Token` header — Telegram's own webhook-security header, not the worker bearer token) receives button taps; `TelegramAdminNotifier.handle_callback` (mirrors the `LlmarktSubmitter` one-service-both-directions pattern) dispatches to the same code paths as the equivalent admin-UI actions (`OllamaServer.pick` + `Task.enqueue_rewrite`/`enqueue_translate`/`enqueue_refine`, `TelegramPoster`, `Translation#archive!`/`unarchive!`), edits the Telegram message in place with a confirmation banner, and logs a new `TelegramAdminNotification` row (chat/message id, last action, who/when) for audit. New `Translation#needs_manual_edit` flag backs action 6 end-to-end (not just a one-way Telegram button): `/admin/translations` gained a "Needs edit" badge + filter toggle and the show page a banner + "Clear flag"/"Flag for manual edit" button (`POST .../toggle_manual_edit`). `bin/rails telegram_admin:set_webhook`/`:delete_webhook` rake tasks register/unregister the webhook with Telegram. 370 tests green (25 new: `TelegramAdminBotTest`, `TelegramAdminNotifierTest`, `Api::TelegramAdminControllerTest`, `Translation`/`Task` coverage). `bin/rails zeitwerk:check` + `rubocop` clean. Smoke-tested live against a disposable copy of the test DB (real server, real login): the "Needs edit" badge/filter, the manual-edit banner, and the toggle action all render/behave correctly; the actual Telegram send/receive side is covered only by stubbed `Telegram::Bot::Client` tests (same convention as `TelegramPosterTest`), not a live bot token. NOTE: dev/prod DBs still need `bin/rails db:migrate` (not run here per project rules) for the `needs_manual_edit` column and `telegram_admin_notifications` table; set `telegram_admin_bot_token`/`telegram_admin_chat_id`/`telegram_admin_webhook_secret` (credentials or ENV) plus `app_base_url`/`APP_BASE_URL`, then run `bin/rails telegram_admin:set_webhook` to activate.
- [x] **Fixed `ArticleImageFetcher` not fetching NYT article images** (no migrations — user prompt: "ArticleImageFetcher is not fetching nyt images"): `ArticleImageFetcher::ALLOWED_HOSTS` (the SSRF host allow-list gating `fetch_og_image`'s HTTP fetch) only ever listed BBC hosts, even though the app has ingested NYT articles (`www.nytimes.com`) since the NYT-feeds feature landed — every NYT article's fetch hit the allow-list guard and returned early, and the resulting `nil` got cached as `""` for the full 1-week `CACHE_TTL`, so NYT stories never got a hero image on the portal or in Open Graph tags. Added `www.nytimes.com`/`nytimes.com` to `ALLOWED_HOSTS`; updated the file's header comment (previously BBC-only wording). Existing NYT articles that already cached an empty-string miss won't retry until that cache entry expires (or is cleared).
- [x] **Telegram admin bot: link buttons + notify only after refine** (no migrations — user prompt: "in telegram add 3 more buttons showing the news in source link, news in our english portal and news in our farsi portal; also only post to telegram admin bot when translation refine is done"): `TelegramAdminNotifier#main_menu` gained three plain `url` link buttons (no callback round-trip) — the source article (`translation.article.url`), the English portal page (`/en/news/:slug`), and the Persian portal page (`/news/:slug`), built off `Llmarkt.app_base_url`; the two portal buttons are skipped when `app_base_url` isn't configured (a relative URL would be rejected by Telegram). `Task#complete!` no longer calls `notify_admin_bot!` for the `"translate"` case — the admin bot now only DMs once a translation has been through the `"refine"` (smart-edit) pass, instead of once per translate *and* once per refine. 384 tests green (task_test.rb updated for the translate/refine notify split; telegram_admin_notifier_test.rb covers the link buttons with/without `app_base_url`). `rubocop`/`zeitwerk:check` clean.
- [x] **Cute in-app "story not found" page for bad/unpublished news links** (no migrations — user prompt: "create a cute page for not found or unpublished news"): a bad slug and an archived translation both raised `ActiveRecord::RecordNotFound`, previously answered with Rails' generic static `public/404.html` (no chrome, no bilingual copy). Added `NewsController#rescue_from ActiveRecord::RecordNotFound, with: :render_story_not_found`, rendering a new `news/not_found` view (status 404) inside the normal `news` layout — nav/sidebar/footer still render since `load_chrome` already ran. New `not_found_*` bilingual copy in `NewsHelper::UI_STRINGS`, a newspaper+dizzy-face icon (Bootstrap Icons, no new assets), and home/search buttons; tagged `noindex, follow` via a new `content_for(:robots)` override point in `layouts/news.html.erb` (previously hardcoded). 387 tests green (3 new). Smoke-tested live in the browser (`bin/rails server`) for both the fa and en editions via Playwright — correct RTL/LTR layout, sidebar still populated, buttons work.
- [x] **Telegram admin bot: skip channel picker with a single channel, keep one-tap confirm** (no migrations — user prompt: "when there is only one telegram channel to forward to, telegram bot should not inquire telegram admin about which telegram channel to forward to"; final clarification: notify must NOT auto-publish, and must not show the submenu for a single channel): `TelegramAdminNotifier#publish_button` renders a direct one-tap post button (`post:<id>:<channel_id>`, "📤 انتشار در <channel name>") when exactly one `TelegramChannel` is enabled — no submenu, and no auto-publish, so the admin still taps once to send (human review gate preserved); posted/re-notified state shows "✅ ارسال شد به <channel name>". Zero/several enabled channels: unchanged picker submenu (`channels:<id>`). Backed out the earlier auto-publish-on-`notify` approach. 394 tests green (2 in `telegram_admin_notifier_test.rb`). `zeitwerk:check` clean.
- [x] **plan2.md — comprehensive code review & strategic improvement plan** (documentation only, no code changes — user prompt: "act as an expert software architect… analyze the entire codebase and create a detailed improvement plan saved as plan2.md; do NOT make any code changes"): read every file in `app/` plus schema/routes/initializers/deploy config and `worker/worker.rb`; ran the test suite (394 green) and Brakeman (1 medium warning) as validation inputs. plan2.md catalogs 6 Critical / 13 High / 15 Medium / 10 Low findings with file:line refs (headliners: deactivated users keep live sessions, no `reset_session` on login, stale-reclaim vs llmarkt duplicate-execution race, `null_session` CSRF override on translations controller, `config.hosts` disabled, blocking geo/og:image HTTP in the public request path, missing indexes, Telegram Markdown escaping), plus refactor proposals, an architecture assessment, 15 prioritized feature ideas with effort estimates, a metrics/validation protocol, and a 5-phase roadmap. CHANGELOG updated. No code/schema/config modified.
- [x] **"Needs Edit" admin sidebar menu — manual-edit queue in one place with a live count badge** (no migrations — user prompt: "create a menu for admin to see all the marked for manual edit items in one place"): manual-edit-flagged translations were previously only reachable by opening `/admin/translations` and toggling the "Needs edit" filter by hand. Added a dedicated **"Needs Edit"** sidebar item (`layouts/admin.html.erb`, under "Translations") linking straight to the pre-filtered queue `admin_translations_path(needs_manual_edit: "1")` — reuses the existing filter/sort/pagination + per-row badge instead of a new controller/view. Carries a yellow live count badge (hidden at zero) from a new memoized `ApplicationHelper#manual_edit_review_count` (`Translation.needs_manual_edit.count`); "Translations" vs "Needs Edit" active-state split on `params[:needs_manual_edit]`. Shown to editors *and* admins (outside the admin-only nav block) since manual edit is the editors' workflow. 391 tests green (5 new in `Admin::TranslationsControllerTest`). `rubocop`/`zeitwerk:check` clean.
- [x] **plan2.md Phase 1 — Pipeline reliability implemented** (migrations `20260716000001` add_unique_index_to_telegram_posts, `20260716000002` create_solid_cable_messages — user prompt: "implement phase 1 and then update plan2 on what is done and not"): all 8 Phase 1 items from plan2.md §10 landed. **C-4** (stale-reclaim vs. llmarkt duplicate-execution race): `Task::LLMARKT_JOB_TIMEOUT` (20 min) is now the single source of truth for the llmarkt job timeout, `STALE_AFTER` derives from it (+10 min buffer, was a flat 15 min — below the llmarkt timeout); `requeue!` clears `external_job_id`; `LlmarktSubmitter.handle_callback`/new `.handle_failure` run under `task.with_lock` and ignore any webhook whose `job_id` doesn't match; `Task#complete!`/`#fail!` are idempotent (`return if completed?`). **H-5**: `complete!` wraps target/article/task writes in one transaction, runs the chain only after commit, with `chain_translate!`/`chain_refine!` now rescued like `chain_autopost!` already was. **H-8**: `TelegramPoster`/`TelegramAdminNotifier` switched `parse_mode: "Markdown"` → `"HTML"` with `CGI.escapeHTML` on every interpolated value. **H-9**: new shared `LlmText.clean` (`app/services/llm_text.rb`) replaces three copies of the `<think>`-stripping regex; `ArticleTranslator.process` — which never stripped reasoning blocks — now uses it too. **H-3**: `FeedFetcher` HTTParty call gets `timeout: 10`. **H-12**: `Task.claim_next!` locks with `"FOR UPDATE SKIP LOCKED"`; `reclaim_stale!` moved outside the claim transaction. **H-11**: added `solid_cable` gem, production `config/cable.yml` now `adapter: solid_cable` folded into the primary DB (same no-separate-database pattern as Solid Cache) via new `solid_cable_messages` table. **M-4**: new `Publisher` service (`app/services/publisher.rb`) consolidates the three near-identical Telegram post/send/update sequences (`Autoposter`, `Admin::TranslationsController#post_to_channel`, `TelegramAdminNotifier#post_to_channel`); new unique index on `telegram_posts(translation_id, telegram_channel_id)` + matching `TelegramPost` validation back its claim-then-post pattern (M-4's 3rd "diverging path" no longer existed — superseded by an earlier session's "skip channel picker" change). plan2.md updated throughout with "✅ DONE"/"✅ PARTIALLY DONE" implementation notes on every Phase 1 finding plus a Phase 1 roadmap status block. 395 tests green (2 updated for the parse-mode switch, 1 new HTML-escaping test, 1 stale tautological test removed). `rubocop`/`zeitwerk:check` clean, Brakeman unchanged (1 known M-9 warning). Verified live via `bin/rails runner`: `complete!` transactionality/idempotency/think-stripping, `requeue!` clearing `external_job_id`, and Solid Cable's `config/cable.yml` resolving correctly. NOTE: dev/prod DBs still need `bin/rails db:migrate` (not run here per project rules) — and if production `telegram_posts` already has duplicate `(translation_id, telegram_channel_id)` rows, those need deduping before that migration will apply. **Not done:** the Phase 1 exit-criteria chaos test (kill worker mid-task, let llmarkt time out, replay duplicate webhooks) wasn't run — fixes were verified individually, not under an actually induced race; Phases 2–4 remain unstarted.
